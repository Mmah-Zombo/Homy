# About

This is a real estate website built with React.

## Start The Site

To start the website after cloning this repository, first install all the dependencies using:

```bash
yarn install
```

Then you run the following command:

```bash
yarn dev
```

In the terminal, you will see the link to the website running locally in your browser.